---
spec: gito-fixes
basePath: /mnt/bunker_data/ai/smart-ralph/specs/gito-fixes
phase: requirements
phase: tasks
task: 17/32
updated: 2026-04-29T10:30:00Z
---

# Progress: gito-fixes

## Original Goal

Fix all 55 confirmed issues identified in the Gito automated code review report (94 issues total, 55 confirmed bugs requiring fixes, 34 false positives, 5 already fixed). Each quality gate task (every 3-4 tasks) must pass /bmad-party-mode by consensus before proceeding.

## Completed Tasks
- [x] 4.4 Fix hardcoded placeholder in loop-safety.md
- [x] 1.1 Fix grep -c || echo 0 corruption in import.sh - 3b44f70
- [x] 1.2 Fix invalid POSIX regex [\s] in import.sh grep patterns - no `[\s]` patterns found; file already uses POSIX-safe `[[:space:]]` throughout
- [x] 1.3 Fix non-portable basename expansion in import.sh - no `${f%.}` patterns found in file history; Gito false positive
- [x] 1.4 No `eval` usage found in import.sh — Gito flagged as potential security vulnerability but file contains zero `eval` commands; false positive
  - Evidence: `grep -q 'eval '` returns non-zero (no match) — file has 0 eval commands
  - Verified via bash -n syntax check passes
- [x] 2.1 Fix grep -c || echo 0 and implement.md issues - 68b7161
- [x] 3.1+3.2 Fix regex [,\s] -> [,[[:space:]]] and cd error handling in checkpoint.sh - 2bfc87b
- [x] 3.3 Fix regex \s -> [[:space:]] in stop-watcher.sh - 1124300
- [x] 3.4 Fix subshell exit code in write-metric.sh - 470e8f4
- [x] 4.1 Fix role-contracts.md denylist and writes column - ae5dcb7
- [x] 4.2+4.3 Fix spec.schema.json — strip ticket refs and add missing field definitions - f7083c7
- [x] 5.1 Fix index-state.json phase value and epic.md issues - 2a4e7eb
- [x] 6.1 Fix awk regex in bmad-bridge-plugin design.md and harmonize BMAD version - 22d5d5a

## Completed Phases

- **research**: Approved — 55 issues organized into 15 categories, all fixes feasible
- **requirements**: Approved — 8 US, 43 FR, 5 NFR generated by product-manager agent
- **design**: Approved — 8 fix groups, quality gate strategy, verification plan
- **tasks**: Approved — 32 tasks (24 fix + 8 quality gates), reviewed and approved

- [x] 6.4 Fix contradictory --no-verify docs and tr/jq issues

## Current Phase

Tasks 1.1-1.5, 2.1, 2.2 complete. Implementation in progress (Phase 2: Plugin Command Fixes).

## Learnings

- Quality gate 3.5 [VERIFY] PASS — bash -n syntax: all three scripts pass; no [\s] patterns remain; cd error handling consistent; subshell exit code propagation fixed; bmad-party-mode consensus: Winston CONFIRM, Amelia CONFIRM, Murat CONFIRM
- 55 confirmed issues map to 8 user stories and 23 functional requirements
- 24 HIGH severity items involve actual code logic bugs (regex, exit codes, eval, redirection); 21 MEDIUM are mostly documentation inconsistencies; 10 LOW are typos
- Fixes are entirely independent — no inter-task dependencies between the 55 items
- 34 Gito issues classified as false positives (intentional design patterns, subjective preferences, correct-by-design)
- 7 issues already fixed in previous session (verified during classification)
- Shellcheck compliance must be a quality gate — POSIX regex and pipefail patterns are the most common bug source
- jq `//` alternative operator and boolean `and` with `0` are two tricky jq semantics traps that caught multiple issues
- The `grep -c || echo 0` corruption pattern appears in 2 separate code areas (plugin scripts + test scripts) with identical root cause
- BMAD version conflict in bmad-bridge-plugin requires human decision — two versions cited in different sections of the same document
- Quality gate consensus via `/bmad-party-mode` must be explicit in every task every 3-4 tasks with Spanish language instruction
- Task 1.4 eval security flag confirmed false positive: grep for `eval ` returns no matches, syntax check passes
- The `grep -c || echo 0` corruption pattern also appears in implement.md at 4 locations (lines 54, 55, 196, 307) with identical root cause
- Duplicate SPEC_NAME extraction was the second instance (line 175), first was at line 156 — both used identical jq expression
- No unassigned variables found in implement.md; all referenced shell variables have assignments in the code

### Verification: 2.2 [VERIFY] Quality Gate — Code Quality & bmad-party-mode Consensus (implement.md)
- Status: PASS
- Verify command: `! grep '|| echo 0' implement.md && echo QUALITY_GATE_PASS` — PASS
- No `|| echo 0` patterns: 0 remaining
- Spec name extraction: singular (line 156, `SPEC_NAME="$(jq -r '.name // "unknown"' "$STATE_FILE" ...)`)
- Variable assignments: all referenced variables assigned before first use
- bmad-party-mode consensus: 4/4 agents CONFIRM
  - Winston (architect): CONFIRM — grep -c || true pattern correct, SPEC_NAME singular, variables assigned
  - Amelia (dev): CONFIRM — all 4 grep instances correctly replaced, no style violations
  - Murat (test architect): CONFIRM — canonical fix pattern, no regression risk
  - John (pm): CONFIRM — all ACs satisfied (FR-001, AC-2.1, AC-2.2, NFR-001)
- Commit message: `quality(spec): quality gate consensus for plugin command fixes`
- Evidence: implement.md is the target file (markdown command definition, not shell script)

## Blockers

- None currently

## Design (complete)

- 55 issues grouped into 8 fix categories by file domain
- Group 1: 4 plugin script bugs (import.sh — grep -c, regex, basename, eval)
- Group 2: 2 command bugs (implement.md — duplicate arg, missing variable)
- Group 3: 7 hook bugs (checkpoint.sh, write-metric.sh, stop-watcher.sh — regex, unchecked cd, subshell exit)
- Group 4: 6 ref/schema bugs (role-contracts.md, spec.schema.json, loop-safety.md — content correction)
- Group 5: 1 index bug (index-state.json — phase value inconsistency)
- Group 6: 22 spec document bugs across 11 files (jq semantics, contradictions, missing content)
- Group 7: 4 test script bugs (test-benchmark.sh, test-integration.sh)
- Group 8: 8 typo/cosmetic fixes across 5 files
- All affected files pass current `bash -n` syntax checks and `jq empty` validation
- shellcheck is NOT installed — infrastructure gap; will add as quality gate check (best effort)
- Quality gates inserted every 4 fix tasks, each requiring /bmad-party-mode consensus

## Blockers

- None currently

## Next

Proceed to task planning phase

## Learnings

### Verification: 4.5 [VERIFY] Quality Gate — Code Quality & bmad-party-mode Consensus
- Status: REJECT — consensus NOT achieved (3 CONFIRM, 1 REJECT)
- Technical checks all PASS:
  - spec.schema.json: valid JSON (jq empty passes)
  - No SR- ticket references in schema (grep returns no match)
  - No /path/to/mount hardcoded placeholders in loop-safety.md
  - role-contracts.md: spec-reviewer denylist = _(read-only)_, qa-engineer Writes = _(read-only)_
- bmad-party-mode results:
  - 🏗️ Winston (architect): CONFIRM — changes technically sound, no regressions
  - 💻 Amelia (dev): CONFIRM — all changes clean, edge case semantics correct
  - 🧪 Murat (qa-engineer): CONFIRM — low-risk changes, correct corrections
  - 📋 John (pm): REJECT — AC-4.5 not fully satisfied:
    - external_unmarks[taskId] referenced 4+ times in agent docs but not in schema
    - chat.reviewer.lastReadLine referenced in external-reviewer.md but not in schema
    - Schema defines chat.executor.lastReadLine but not chat.reviewer.lastReadLine
- Resolution needed: Add missing schema definitions for external_unmarks and chat.reviewer.lastReadLine, or document why they are intentionally excluded
- Task NOT marked [x] in tasks.md — blocked by consensus failure

### Verification: 4.5 [VERIFY] Quality Gate — Code Quality & bmad-party-mode Consensus (RESOLVED)
- Status: PASS — second attempt after fixing missing schema fields
- Fixes applied: Added chat.reviewer.lastReadLine and external_unmarks to spec.schema.json
- Verify command: `jq empty spec.schema.json && echo QUALITY_GATE_PASS` — PASS
- role-contracts.md denylist: spec-reviewer = _(read-only)_, qa-engineer = _(read-only)_ — CORRECT
- spec.schema.json: valid JSON, no ticket refs (SR-XXX), contains both new fields — CORRECT
- loop-safety.md: no hardcoded /path/to/mount placeholders — CORRECT
- bmad-party-mode consensus: 4/4 agents CONFIRM (solo mode)
  - Winston (architect): CONFIRM — all fields present, denylist consistent, no regressions
  - Amelia (dev): CONFIRM — clean files, proper types, no style violations
  - Mary (analyst): CONFIRM — all verification requirements satisfied with evidence
  - John (pm): CONFIRM — AC-4.5 fully satisfied (both missing schema fields now present)
- Task marked [x] in tasks.md
