interface:
  display_name: "RalphHarness"
  short_description: "Run RalphHarness workflows in Codex"
  default_prompt: "Use $ralphharness to run Ralph step by step in this repo and ask me to `approve current artifact`, `request changes`, or `continue to <named next step>`."
policy:
  allow_implicit_invocation: false
